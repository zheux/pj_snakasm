%include "_map.mac"

%define SNAKE 4
%define SNAKE.STARTPOSITION_HEAD MAP.TOPLEFT + COLS * 2 * (MAP.ROWS / 2) + 2 * (MAP.COLS / 2 + 3)
%define SNAKE.STARTPOSITION_TAIL SNAKE.STARTPOSITION_HEAD - 160

%macro bind_movement 2
   cmp byte [direction], %1
   jne %%next
   call %2
   %%next:
%endmacro
